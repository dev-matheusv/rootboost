# .NET
bin/
obj/
*.user
.vs/

# EF Core / SQLite local db (persist via volume in prod, never commit)
*.db
*.db-shm
*.db-wal

# Secrets — NEVER commit
.env
appsettings.*.local.json
secrets.json

# Credencial e token da CJ (ferramenta de descoberta local) — NEVER commit
tools/cj/creds.json
tools/cj/.token.json

# OS / editor
.DS_Store
Thumbs.db

# local-only sandbox test (never commit)
landing/rack/_local/

# video pesado da landing (comprimir antes de commitar)
landing/car/media/*.mp4
