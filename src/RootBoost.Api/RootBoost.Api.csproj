<Project Sdk="Microsoft.NET.Sdk.Web">

  <ItemGroup>
    <ProjectReference Include="..\RootBoost.Infrastructure\RootBoost.Infrastructure.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- Ship catalog.json next to the app so it resolves at runtime. -->
    <None Include="..\..\catalog.json" Link="catalog.json" CopyToOutputDirectory="PreserveNewest" />
  </ItemGroup>

  <PropertyGroup>
    <TargetFramework>net9.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
  </PropertyGroup>

</Project>
